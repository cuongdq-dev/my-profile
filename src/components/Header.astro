---
import { getTranslation } from "../utils/ui";

const facebookUrl = import.meta.env.FACEBOOK_URL;
const githubUrl = import.meta.env.GITHUB_URL;
const phoneNumber = import.meta.env.PHONE_NUMBER;
---

<section class="header_area">
  <div class="header_navbar">
    <div class="container">
      <div class="row">
        <div class="col-lg-12">
          <nav class="navbar navbar-expand-lg">
            <a class="navbar-brand" href="index.html">
              <img src="assets/images/logo.svg" alt="Logo" />
            </a>
            <button
              class="navbar-toggler"
              type="button"
              data-toggle="collapse"
              data-target="#navbarSupportedContent"
              aria-controls="navbarSupportedContent"
              aria-expanded="false"
              aria-label="Toggle navigation"
            >
              <span class="toggler-icon"></span>
              <span class="toggler-icon"></span>
              <span class="toggler-icon"></span>
            </button>

            <div
              class="collapse navbar-collapse sub-menu-bar"
              id="navbarSupportedContent"
            >
              <ul id="nav" class="navbar-nav ml-auto">
                <li class="nav-item active">
                  <a
                    data-i18n={getTranslation("nav.home")}
                    class="page-scroll"
                    href="#home">Home<span></span></a
                  >
                </li>
                <li class="nav-item">
                  <a
                    data-i18n={getTranslation("nav.about_us")}
                    class="page-scroll"
                    href="#about">About<span></span></a
                  >
                </li>
                <li class="nav-item">
                  <a
                    data-i18n={getTranslation("nav.service")}
                    class="page-scroll"
                    href="#services">Services<span></span></a
                  >
                </li>
                <li class="nav-item">
                  <a
                    data-i18n={getTranslation("nav.project")}
                    class="page-scroll"
                    href="#project">Portfolio<span></span></a
                  >
                </li>
                <li class="nav-item">
                  <a
                    data-i18n={getTranslation("nav.experience")}
                    class="page-scroll"
                    href="#work">Experience<span></span></a
                  >
                </li>
                <!-- Language Select Dropdown -->
                <select id="langSelect" class="ml-4 lang-select">
                  <option class="nav-item" value="en">🇺🇸 English</option>
                  <option class="nav-item" value="vi"> 🇻🇳 Tiếng Việt</option>
                </select>
              </ul>
            </div>
          </nav>
          <!-- navbar collapse -->
        </div>
        <!-- navbar -->
      </div>
    </div>
    <!-- row -->
  </div>
  <!-- container -->
</section>
<!-- header navbar -->

<div id="home" class="header_hero">
  <ul class="header_social d-none d-lg-block">
    {
      facebookUrl && (
        <li>
          <a target="_blank" href={facebookUrl}>
            <i class="lni lni-facebook-filled" />
          </a>
        </li>
      )
    }
    {
      githubUrl && (
        <li>
          <a target="_blank" href={githubUrl}>
            <i class="lni lni-github" />
          </a>
        </li>
      )
    }
    {
      phoneNumber && (
        <li>
          <a href={`tel://${phoneNumber}`}>
            <i class="lni lni-phone" />
          </a>
        </li>
      )
    }
  </ul>
  <div class="container">
    <div
      class="row align-items-center justify-content-center justify-content-lg-between"
    >
      <div class="col-lg-6">
        <div class="header_hero_content mt-45">
          <h5
            data-i18n={getTranslation("header.sub_title")}
            class="header_sub_title wow fadeInUp"
            data-wow-duration="1.3s"
            data-wow-delay="0.2s"
          >
          </h5>
          <h2
            data-i18n={getTranslation("header.title")}
            class="header_title wow fadeInUp"
            data-wow-duration="1.3s"
            data-wow-delay="0.5s"
          >
          </h2>
          <span
            data-i18n={getTranslation("header.description")}
            class="wow fadeInUp"
            data-wow-duration="1.3s"
            data-wow-delay="0.8s"></span>
          <p
            data-i18n={getTranslation("header.sub_description")}
            class="wow fadeInUp"
            data-wow-duration="1.3s"
            data-wow-delay="1.1s"
          >
          </p>
          <a
            href="https://rebrand.ly/freelancer-ud"
            rel="nofollow"
            data-i18n={getTranslation("header.button")}
            class="main-btn wow fadeInUp"
            data-wow-duration="1.3s"
            data-wow-delay="1.4s"></a>
        </div>
        <!-- header hero content -->
      </div>
      <div class="col-lg-6 col-md-6 col-sm-7">
        <div
          class="header_hero_image mt-50 wow fadeInRightBig"
          data-wow-duration="1.3s"
          data-wow-delay="1.8s"
        >
          <img src="assets/images/hero.png" alt="hero" />
        </div>
        <!-- header hero image -->
      </div>
    </div>
    <!-- row -->
  </div>
  <!-- container -->
  <div class="header_hero_shape d-none d-lg-block"></div>
  <!-- header hero shape -->
</div>
<!-- header hero -->

<script>
  // Language select change event
  document.addEventListener("DOMContentLoaded", function () {
    const langSelect = document.getElementById(
      "langSelect"
    ) as HTMLSelectElement | null;

    if (langSelect) {
      // ✅ Get the saved language from localStorage or set "en" as default
      const savedLang = localStorage.getItem("lang") || "en";

      // ✅ Set the default value of <select>
      langSelect.value = savedLang;

      // ✅ Listen for changes and update localStorage
      langSelect.addEventListener("change", function () {
        const selectedLang = langSelect.value;
        localStorage.setItem("lang", selectedLang);
        window.dispatchEvent(new Event("languageChange"));
        window.dispatchEvent(new Event("storage"));
      });
    }
  });
</script>
